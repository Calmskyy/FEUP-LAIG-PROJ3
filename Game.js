/**
 * Class that holds all the functions related to the game logic
 * @constructor
 * @param scene blahblah
 */
class Game {
	constructor(scene) {
		this.scene = scene;
	};

	/**
	 * @method apply
	 * Does something
	 */
	apply() {
		return super.apply();
	}
};